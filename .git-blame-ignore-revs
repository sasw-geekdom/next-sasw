# Revisions git blame should look straight through.
#
# A formatting pass rewrites lines without changing what they do, so without
# this every one of those lines blames to the pass instead of to the commit
# that actually wrote it. GitHub reads this file automatically; locally it
# needs one command, once:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only ever add a commit here that provably changed no behaviour.

# Format the repo, and stop prettier from reaching the lockfile
ed8d10e18350272a57c82ca2ab230e0d1e2718c2
